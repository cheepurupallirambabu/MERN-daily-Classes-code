Css Notes:
 - css (cascading style sheet) to apply styling , layouts, animations to the web pages
 - css are in the year 1994 by the developer "hakon wium lie"
 - the prasent version of css is css3
 - methods to apply styling: 
   1. inline css
     - useing style attribute to apply style in html element
     - applicable for only that element
     - css selectors are not required
     - it is more priority then internal and external css
   2. internal css
      - by useing <style></style> tag in head prasent
      - it needs css selectors
   3. external css
      - apply styling by taking new .css file and link to html file 
         useing <link> tag
      - it needs css selectors


css selectors:
 - which are used to target html Elements to apply styling
 - types of css selectors:
   1. simple selectors:
      - id(#),class(.), tag, grouping(,), universal(*) selector
   2. combinator selectors
     - descendent selector (space)
     - child selector (>)
     - adjacent sibling selector(+)
     - general sibling selector(~)
   3. pseudo class selectors
     - structural pseudo class selectors
       - :first-child
       - :last-child
       - :first-of-type
       - :last-of-type
       - :nth-child(n)
     - dynamic pseudo class selectors
         - :hover
         - :active
         - :visited  -> link tag
         - :focus  -> input tag

   4. pseudo element selectors
     - ::after 
     - ::before
     - ::first-line
     - ::first-letter
     - ::selection
     - ::placeholder
     - ::maker
   5. attribute selectors
     - which are used to target html elements based on attributes

     eg: <h1 align="center" id="head">hello</h1>
     <h1 id="head">hiiii</h1>

     types:
     - [attributeName="value"]
     - [attributeName^="value"]
     - [attributeName-="value"]
     - [attributeName*="value"]
     - [attributeName$="value"]

color values:
  - colorname
  - rgb (red,gree,blue)
  - rgba(red,green, blue,alpha)
  - hsl(hue,saturation ,lightness)
  - hsla(hue,saturation ,lightness,alpha)
  - hexa code
  - transparent
  - color()

 text properties:
   - text-alignment
      - text-align
      - word-space
      - latter-spaceing
      - text-indent
      - line height
   - text decoration :
     - text-decoration-line
     - text-decoration-color
     - text-decoration-style
     -  text-decoration-thickness
     -  text-decoration()

  - text transform properties:
    - uppercase()
    - lowercase()
    - capitalize()
css boxs models:
     - padding
         - padding-top
         - padding-right
         - padding-bottom
         - padding-left
         - padding
     - border
         - border-top
         - border-right
         - border-bottom
         - border-left
         - border

         - border-style
         - border-color
         - border-width
         - border-radius
             - border-top-left-radius
             - border-top-right-radius
             - border-bottom-left-radius
             - border-bottom-right-radius
             - border-radius
     - margin
         - margin-top
         - margin-right
         - margin-bottom
         - margin-left

flex-properties:
   - display: flex;
   - flex-decoration : 
   - flex-wrap
   - justify-content
   - align-items
   - flex-grow
   - flex-shrink
   - flex

position proprties:
   - position : static, reletive, absolute , fixed, sticky
   - left, right, top, bottom

animations 
  - define animations using @keyframes
  - link that animation to html element

grid properties:
  - display : grid;
  - grid-template-column
  - grid-template-row
  - row gap
  - column-gap

  - grid-row-satrt
  - grid-row-end
  - grid-column-start
  - grid-column-end

media queries :
   - to build responsive webpages based on device width
   - break points 
      - smaller devices 0px - 450px
      - medium devices 450px - 850px
      - large devices 850px - 1200px
      - extra large devices >1200px 





   